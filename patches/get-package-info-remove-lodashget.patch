*** Begin Patch
*** Add File: patches/get-package-info-remove-lodashget.patch
+*** From 0000000000000000000000000000000000000000 Mon Sep 17 00:00:00 2001
+*** Update File: index.js
+@@
-const get = require('lodash.get');
+// Replaced lodash.get with a tiny internal `get` helper to avoid a deprecated dependency.
+function get(obj, path, def) {
+  if (!path) return obj ?? def;
+  const parts = String(path).split('.');
+  let cur = obj;
+  for (let i = 0; i < parts.length; i++) {
+    if (cur == null) { cur = undefined; break }
+    const p = parts[i];
+    // support array indices like 'a[0]'
+    const m = p.match(/(.+)\[(\d+)\]$/);
+    if (m) {
+      cur = cur[m[1]];
+      cur = cur == null ? undefined : cur[Number(m[2])];
+    } else {
+      cur = cur[p];
+    }
+  }
+  return cur == null ? def : cur;
+}
*** End Patch
+